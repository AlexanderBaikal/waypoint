/**
 * Drawings shared across features, in a 16px box.
 *
 * The bookmark is the one mark the interface says twice: the button that saves
 * a place, and the chip that filters the list down to what was saved. They have
 * to be the same shape, or the chip does not read as the thing you pressed.
 */
export const BOOKMARK = "M4 2.6h8v11.2L8 10.8l-4 3V2.6Z";
